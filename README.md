```Javascript
/**
 *  Intro?
 */
 
const user     = [ "acayrin", "acay" ]
const lang     = [ "vn_vi", "us_en" ]
const age      = -1
const gend     = undefined
const dev_lang = [ "js", "php", "html", "css", "java", "anything left to learn" ]
const dev_pf   = [ "heroku", "aws", "vercel", "localhost" ]
const dev_id   = [ "vscode", "atom", "gedit" ]
const yes      = no
const meme     = "r/FiftyFifty"
const games    = Games.filter(G => G.platform === "mobile")
```
